extends ../main-template

block description
    title Aktivace účtu | Červená tlapka
    meta(name="description", content="Pokud nemáte aktivovaný profil, zde si můžete nechat zaslat aktivační email.")

block content
    include ../includes/forms
    include ../includes/recaptcha
    div.w3-container.w3-row-padding
        div.w3-half
            - var data = templateData.data;
            - var errors = templateData.errors;
            h1 Aktivace účtu
            p Zadejte prosím Váš email, který jste uvedli při registraci. Po odeslání formuláře Vám na něj budou odeslány pokyny pro aktivaci účtu.
            form(method='post', action='/activate-profile/email')
                +input("text", "email", "Email:", data, errors)
                div.w3-container.w3-section
                    +recaptchaInputv2()
                    +submit()
                //+recaptchaScript("resendActivateProfileEmail")
        div.w3-half
            p Pokud nemáte založený účet, #[a(href='/register') zaregistrujte se prosím].